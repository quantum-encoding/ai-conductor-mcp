Convert text to speech using Quantum AI. Use the text_to_speech MCP tool: $ARGUMENTS

Default voice is "alloy". Available voices: alloy, echo, fable, onyx, nova, shimmer. Save the audio file to the current project if appropriate.
