Generate an image using Quantum AI. Use the generate_image MCP tool with a detailed prompt based on the user's request: $ARGUMENTS

If no specific model is mentioned, use grok-imagine-image. Save the result to the current project directory if appropriate.
